# Tailr is bring-your-own-key — paste your Gemini API key into the
# Settings → API Key panel inside the running app, not into this file.
# This file exists only for the optional PORT override below.

# Optional: API server port (default 3001).
# PORT=3001
